{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "gitRefresh": "git rm -r --cached . && git add .",
    "update": "npm update && npx @astrojs/upgrade && npm prune && cd ../home && npm update && npx @astrojs/upgrade && npm prune && cd ../nox && npm update && npx @astrojs/upgrade && npm prune && cd ../main-site"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.0",
    "@fontsource-variable/atkinson-hyperlegible-next": "^5.2.4",
    "@fontsource-variable/inter": "^5.2.5",
    "@fontsource-variable/noto-sans": "^5.2.6",
    "@fontsource-variable/sometype-mono": "^5.2.5",
    "astro": "^5.13.7"
  }
}
